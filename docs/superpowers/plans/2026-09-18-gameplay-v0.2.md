# GAMEPLAY v0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Réécrire `docs/GAMEPLAY.md` en v0.2 (transport pods/slots, Hydroïdes narratifs, défaite spirale) + corriger `PFD.md` et `DATA.md` selon la spec 2026-09-18-gameplay-v0.2.

**Architecture:** Jalon **docs uniquement** — les `data/*.json` (16 ressources, 46 items) et la sim (`src/simulation/`) sont déjà alignés ou inchangés. Chaque tâche réécrit une section de doc, vérifiée par cohérence interne (références croisées + compteurs) et consignée. Règles du repo : français, tables markdown uniformes, valeurs copiées de la spec/du data, références `docs/`.

**Tech Stack:** Markdown (docs), tables markdown, `git` local (rtk), pas de code TypeScript exécuté.

## Global Constraints

- Spec source : `docs/superpowers/specs/2026-09-18-gameplay-v0.2-design.md` (toute valeur ci-dessous en est copiée).
- Transport : vaisseau = `capacité` (t) + `slots` ; slot homogène `{ ressource|item, quantité, masse unitaire }` ; types `supply` (max 250/chargement), `tool` (item `toolPod`), `cryo` (équipe, pod Cryo) ; masse = contrainte `Σ(q×masse) ≤ capacité` ; carburant `coût = f(distance, masse)` ; **MeH** au sol/intra-système, **HeD** en orbite+FTL.
- Vaisseaux transporteurs : Navette (1 slot un, 100 t, MeH, intra-système), IOS (3 slots : 1 `tool`+2 `supply`, 2 500 t, MeH, inter-planètes), SCG (3 slots de même répartition, 5 000 t, HeD, FTL).
- Vaisseaux de combat v0.1 (sonde, briseur, frégate, croiseur) : **conservés inchangés** (remodelage = v0.3 hors périmètre).
- Ressources : **16** (Fe, Ti, Al, C, Cu, H, D, CH₄, He, Pd, Pt, Ag, Au, Si, MeH, HeD) — les 2 carburants sont des produits composés, non minables.
- Hydroïdes : message crypto jour 300 (révèle 8 segments) + bulletins espacés ~90 j (pur fluff) ; 8 segments = artefacts (Atlantic, Chloé, Babylone, Hadrien, Romulus, Césius, Pliocène, Alpha) ; pas de faction jouable.
- Défaite : (a) perte de la Terre-Ville ou (b) effondrement (toutes installations de production détruites/occupées ET aucune unité de combat opérationnelle) ; spirale par « Prises de position » (seuil `M` + guerre : 6ᵉ usine orbitale ou laser Méthanoïde) qui remplacent les derricks par des ruines ; pas de compte à rebours.
- Valeurs techniques extraites de RE (à refléter dans les docs) : plafond de stock 50 000 t/ressource ; départs la navette : Land 2 j / TakeOff 5 j / réparation 2 j ; les 46 items ont masse, `toolPod`, `orbitOnly`.
- Documents à ne PAS modifier sans tâche : `src/`, `data/*.json`, `docs/GAMEPLAY.md` autres sections hors périmètre, `docs/DASHBOARD.md` (touché seulement dans la consignation T6).
- Noms de fichiers exacts (copier-collé) : `docs/GAMEPLAY.md`, `docs/PFD.md`, `docs/DATA.md`, `docs/DECISIONS.md`, `docs/TRACE.md`, `docs/DASHBOARD.md`, `docs/RESEARCH.md`.
- Rédaction en français ; commiter un seul fichier par commit ; style de message `docs(...)`.

---

### Task 1: GAMEPLAY.md — temps & économie (v0.2)

**Files:**
- Modify: `docs/GAMEPLAY.md` (§1, §2.1, §2.2, §2.3, §2.4)

**Interfaces:**
- Consumes: spec GAMEPLAY v0.2 §2, §3, §6.1 (table 16 ressources, slots, valeurs transport).
- Produces: les §1-2 doivent être cohérents avec les valeurs des ressources minables (`derrickRate`, `surveyMultiplier` dans `data/resources.json`) et servir de référence aux Tasks 2-5.

- [ ] **Step 1: Ajuster l'en-tête et l'ordre des ticks**

Remplacer l'en-tête :
```markdown
> Version 0.1 — 2026-09-15
> Complément chiffré du [PFD](PFD.md). Les valeurs ci-dessous sont des **premières itérations** à valider par playtests.
```
par :
```markdown
> Version 0.2 — 2026-09-18
> Complément chiffré du [PFD](PFD.md). Arbitrages posés : transport pods/slots (hybride UX), Hydroïdes narratifs, défaite par spirale (spec `docs/superpowers/specs/2026-09-18-gameplay-v0.2-design.md`).
```
Vérifier que l'ordre des ticks (§1) reste : événements → recherche → production → minage → logistique → navigation → combat → entretien → nouvelles. (inchangé)

- [ ] **Step 2: Réécrire §2.1 (16 ressources)**

Remplacer toute la table "Ressources de base" (10 lignes exotiques actuelles — Titane, Carbone, Palladium, Argent, Iridium, Eau, Deutérium, Uranium, Hélium-3, Terres rares) par :

```markdown
### 2.1 Ressources de base

16 matières (enum original). Les deux carburants composés **MeH** et **HeD** sont des **produits de recette** (non minables) ; tout le reste est minable aux derricks.

| Ressource | Symbole | Dérrick/j | Survey × | Minable |
|---|---|---|---|---|
| Fer | Fe | 2 | 1 | oui |
| Titane | Ti | 2 | 1 | oui |
| Aluminium | Al | 2 | 1 | oui |
| Carbone | C | 2 | 1 | oui |
| Cuivre | Cu | 2 | 1 | oui |
| Hydrogène | H | 1 | 1 | oui |
| Deutérium | D | 1 | 1 | oui |
| Méthane | CH₄ | 1 | 1 | oui |
| Hélium | He | 1 | 4 | oui |
| Palladium | Pd | 1 | 1 | oui |
| Platine | Pt | 1 | 2 | oui |
| Argent | Ag | 1 | 2 | oui |
| Or | Au | 1 | 3 | oui |
| Silice | Si | 2 | 1 | oui |
| Carburant MeH | MeH | — | — | non (H×2 + CH₄×2, toute base) |
| Carburant HeD | HeD | — | — | non (He×2 + D×2, orbite seule) |
```
> Valeurs : `data/resources.json` + RESEARCH §9. `MeH` : toute base planétaire ; `HeD` : usine en orbite uniquement.

- [ ] **Step 3: Mettre à jour §2.2 (minage)**

Remplacer la **liste à puces** actuelle de §2.2 par :
```markdown
### 2.2 Production minière

- Extraction/jour = `derrickRate` (table §2.1) × nombre de derricks ; le tout soumis au gisement et à son épuisement.
- Gisement : réserve `R` (t) ; à `R=0` la mine cesse (épuisé). Stock local plafonné à **50 000 t par ressource** (RE original).
- Prospection : un scan (sonde/dérrick) révèle une ressource au survey : `SurveyTicks = aléa(0..8) × surveyMultiplier` (table §2.1).
```

- [ ] **Step 4: Réécrire §2.3 (transport pods/slots)**

Remplacer tout le §2.3 actuel par :
```markdown
### 2.3 Transport pods/slots

- Un vaisseau = **`capacité` (t)** + **`slots`** ; un slot porte **une cargaison homogène** `{ ressource | item, quantité, masse unitaire }`.
- Types de slots :
  - **`supply`** : ressource en vrac (Fe, Ti, MeH…) — max **250/chargement** ;
  - **`tool`** : un item ayant le flag `toolPod` (derrick, OF frame, ACC, Grapple, AMA, R Frame…) — 1 type + quantité ;
  - **`cryo`** : personnel (1 slot = 1 équipe) — débloqué par le pod Cryo.
- **Masse = contrainte de charge** : `Σ (quantité × masse unitaire) ≤ capacité`. C'est elle qui borne le passage à l'échelle, pas le nombre de slots.
- **Carburant** : consommé à chaque voyage, `coût = f(distance, masse chargée)` ; **MeH** au sol / intra-système, **HeD** en orbite et FTL.
- Temps de manœuvre de la navette : atterrissage 2 j, décollage 5 j, réparation 2 j. Durées de voyage : intra-système `max(|Δordre|, 1)` j, inter-planètes `|Δordre|×4` j (différence d'ordre des corps, RE).
- **Transducteur de masse (TMT, tech exotique)** : envoi instantané mais coûteux — items matériels transportables uniquement (pas les êtres).
```

- [ ] **Step 5: Réécrire §2.4 (routage)**

Remplacer le §2.4 actuel par :
```markdown
### 2.4 Routage & automatisation

- **Envoi manuel** : « envoyer X de R vers corps C » → le vaisseau décolle dès chargement complet (slots et capacité respectés).
- **Routes de ravitaillement** (pré-ACC) : une station déclare « si stock ≥ seuil, transporter X de R vers C » avec vaisseau affecté ; répété chaque tick.
- **ACC** (item auto-cargo d'un vaisseau) : charge/décharge automatique des slots `supply` d'un vaisseau attitré (max 250).
- **AOC** (`autoProduce`) : la production d'une usine tourne seule (déjà dans la table items).
- Implémentation sim de ces automatismes = EPIC future (hors GAMEPLAY v0.2).
```

- [ ] **Step 6: Vérifier la cohérence de la section**

Lancer : `rg -n "Iridium|Eau|Uranium|Terres rares|coque|panneau|solaire" docs/GAMEPLAY.md`
Attendu : **aucune** occurrence de l'ancienne nomenclature ressources ni des recettes exotiques dans §1-2 (déplacer la vérif pour la cohérence globale du §1-2).

- [ ] **Step 7: Commit**

```bash
git add docs/GAMEPLAY.md
git commit -m "docs(gameplay): v0.2 temps & économie (16 ressources, transport pods/slots, routage)"
```

---

### Task 2: GAMEPLAY.md — recherche, production, constructions, vaisseaux (v0.2)

**Files:**
- Modify: `docs/GAMEPLAY.md` (§3, §4, §5.1, §5.2)

**Interfaces:**
- Consumes: spec §2, §6.1 ; résultat des §1-2 (ressources 16, transport pods/slots).
- Produces: §3-6 alignés sur `data/items.json` (46 items, masses, `toolPod`, `orbitOnly`).

- [ ] **Step 1: Réécrire §3 (recherche)**

Remplacer les **exemples de la table §3** par des catégories inchangées (Construction, Propulsion, Énergie, Industrie, Combat, Biologie, Exotique) mais ajouter au texte :
```markdown
- Recherche : un seul projet courant (géré depuis la Terre) ; progression `v = (équipe × 2^rang) × multiplicateur / 801`, wrap à 100 % (RE). Eureka bonus +X %.
- **Milestones** : certains contenus (Hyperlight, TMT, SCG…) ne sont débloquables que par événements temporels (gates timeline), pas par les seuls points.
- **Rétro-ingénierie** : capturer une tech Méthanoïde la débloque à coût moindre.
```

- [ ] **Step 2: Réécrire §4 (production / recettes = items v1)**

Remplacer l'exemple de table "Recettes (v1)" par un renvoi à la table items de vérité :
```markdown
### 4. Production / Manufacture

- Recette : `inputs` (16 ressources) → `outputs`, temps `T` (jours). L'usine consomme la recette complète au départ.
- Les **46 items** (dont dérricks, châssis/drives, pods, carburants) ont une **masse** (contrainte de transport, §2.3) et un mode orbite (`orbitOnly`) : **tout ce qui est gros (châssis I/G, drives stellaires, drones, PTL…) se construit uniquement en orbite**.
- Référence exhaustive : table RESEARCH §9 (46 items, coûts/masses) — ne pas dupliquer ici.
- Atelier : 1 lot/T, coût humain ; Automate : 2 lots/T, consomme énergie + maintenance.
```
> Note : ne pas copier les 46 lignes dans GAMEPLAY ; pointer RESEARCH §9 (DRY, une seule table de vérité).

- [ ] **Step 3: Conserver §5.1 (structures) inchangé**

- Vérifier qu'aucune refonte de §5.1 n'est nécessaire : `rg "Station orbitale|Mine (surface)|Chantier spatial" docs/GAMEPLAY.md` — ligne présente et inchangée.
- (Aucune modification.)

- [ ] **Step 4: Réécrire §5.2 (vaisseaux)**

Remplacer la table **§5.2** par :
```markdown
### 5.2 Vaisseaux

**Transporteurs** (pods/slots, §2.3) :

| Vaisseau | Slots | Capacité (t) | Carburant | Portée |
|---|---|---|---|---|
| Navette | 1 (`supply` ou `tool`) | 100 | MeH | intra-système + atterrissage |
| IOS (Interplanetary Ops) | 3 (1 `tool` + 2 `supply`) | 2 500 | MeH | inter-planètes |
| SCG (FTL) | 3 (1 `tool` + 2 `supply`) | 5 000 | HeD | inter-systèmes |

**Combat / outillage** (conservé de v0.1, remodelage à l'originale = v0.3) :

| Vaisseau | Coût (t) | Role |
|---|---|---|
| Sonde | 2 Ti, 1 C | exploration passive |
| Briseur d'astéroïde | 25 Ti, 10 Ir | minage ceinture |
| Frégate | 20 Ti, 10 Pd, 5 Ir | combat |
| Croiseur | 60 Ti, 20 Pd, 10 Ir, 5 TR | combat lourd |
```
> ⚠️ Cohérence : cette table utilise les anciennes ressources exotiques (Ir, TR, Pd…) qui n'existent plus en §2.1. Voir **Step 5** pour la résolution — on garde ces exemples en v0.1 markée « à recaler en v0.3 », ou on les convertit en items v1 (châssis/vaisseau = items I/OF...). C'est une décision de cohérence : **je tranche** → garder le tableau de combat tel quel avec une note « coûts provisoires v0.1, à refaire avec la table items (v0.3) ».

- [ ] **Step 5: Poser la note de cohérence**

Ajouter en dessous du tableau combat :
```markdown
> ⚠️ Coûts du tableau combat issus de v0.1 (ressources exotiques remplacées) : **provisoires**, recalés sur la table items 46 à la v0.3 (combat).
```

- [ ] **Step 6: Vérifier la cohérence**

Lancer : `rg -n "Iridium|Eau|Uranium|Terres rares|Hélium-3" docs/GAMEPLAY.md` et `rg -n "coque vaisseau|panneau solaire|LOx" docs/GAMEPLAY.md`
Attendu : les seules occurrences restantes sont la **note provisoire** du tableau combat (§5.2).

- [ ] **Step 7: Commit**

```bash
git add docs/GAMEPLAY.md
git commit -m "docs(gameplay): v0.2 recherche/production/vaisseaux (table items, Navette-IOS-SCG)"
```

---

### Task 3: GAMEPLAY.md — IA Méthanoïde, timeline Hydroïdes, défaite (v0.2)

**Files:**
- Modify: `docs/GAMEPLAY.md` (§7, §8, §9, + nouveau §10 Défaite)

**Interfaces:**
- Consumes: spec §4, §5, §6.1.
- Produces: §7-10 (nouvelle section Défaite).

- [ ] **Step 1: Réécrire §7 (IA Méthanoïde + Prises de position)**

Remplacer les 4 puces actuelles de §7 par :
```markdown
### 7. IA Méthanoïde

- Niveau de menace `M` croît si le joueur attaque les Méthanoïdes (ou les ignore trop longtemps). Seuil : raids, puis **Prises de position** (voir §10).
- **Guerre déclenchée par les actes du joueur** (pas aléatoirement) : **6ᵉ usine orbitale construite** ou **obtention du laser Méthanoïde** (commerce).
- **Raids** : attaques de convois et de points faibles → pertes de vaisseaux.
- **Prises de position** (à partir d'un seuil `M` + guerre déclarée) : invasion d'une base → les **derricks sont remplacés par des ruines** (productivité amputée), reconstruction coûteuse.
- Pacte « négocié » (via comms/scénario) : baisse temporairement `M`.
```

- [ ] **Step 2: Réécrire §8 (timeline Hydroïdes) + sous-section Segments**

Remplacer §8 par :
```markdown
## 8. Événements scénarisés (timeline v0.2)

| Jour jeu | Événement |
|---|---|
| 1 | Début de partie, Terre-Ville opérationnelle |
| 15 | Premier contact avec une colonie lointaine |
| 60 | Découverte d'artefacts Mohammed sur la Lune (indice FTL) |
| 120 | Première incursion Méthanoïde (si M positif) |
| 300 | **Message des Hydroïdes (crypto)** : « Nous avons transmuté un don aux Méthanoïdes. Ils l'ont démonté, les segments sont éparpillés dans 8 étoiles. » |
| +90 j (récursif) | Bulletins Hydroïdes épisodiques (fragments chiffrés : indices segments, avertissements) |
| (variable) | Déblocage possible de la fin de partie |

### 8.1 Segments Hydroïdes

- Les **8 segments** sont des **artefacts Hydroïdes** (la « machine » démontée par les Méthanoïdes), un par système extrasolaire : Atlantic, Chloé, Babylone, Hadrien, Romulus, Césius, Pliocène, Alpha.
- Objectif de fin de partie : les récupérer tous → victoire (clip final). Chaque segment récupéré est consigné (flags + bulletin).
- Ils sont **fragiles à préserver** : détruire/abîmer un segment compromet la victoire.
```

- [ ] **Step 3: Conserver §9 (difficultés)**

- Vérifier le tableau des difficultés inchangé (Découverte +50 %/×0,6, Commandant +20 %/×1,0, Vétéran 0 %/×1,4, Méthanoïde −20 %/×1,8). Ajouter la note :
```markdown
> Le « on perd ses deux premières parties » n'est pas une règle : c'est la difficulté par défaut (Vétéran/Méthanoïde) qui l'exprime.
```

- [ ] **Step 4: Créer le § 10 (Défaite)**

Ajouter en fin de document :
```markdown
## 10. Défaite

**Game over si l'une des deux conditions :**

1. **Perte de la Terre-Ville** — l'invasion de la Terre par les Méthanoïdes réussit (défense submergée).
2. **Effondrement économique et militaire** — toutes les installations de production (stations, bases, derricks) sont détruites ou occupées **ET** aucune unité de combat opérationnelle ne subsiste.

### 10.1 Spiral (mécanique)

- Elle se construit par **prises de position** successives (§7) : chaque base perdue réduit la production, donc la capacité de reconstruction et de défense.
- La reconstruction d'un relais qui coûte plus que ce que rapporte l'économie restante = zone de défaite par **inertie** — pas de compte à rebours.
- Ordre d'explosion préféré de l'original : les Méthanoïdes dégradent l'infrastructure, pas seulement les flottes.
```

- [ ] **Step 5: Vérifier la cohérence**

Lancer : `rg -n "MAD|bouclier|capture|66" docs/GAMEPLAY.md` — s'assurer que les mécaniques de combat §6 sont bien **inchangées** et non altérées par §7.
Attendu : §6 toujours présent tel quel.

- [ ] **Step 6: Commit**

```bash
git add docs/GAMEPLAY.md
git commit -m "docs(gameplay): v0.2 IA, timeline Hydroïdes + segments, défaite spirale"
```

---

### Task 4: PFD.md — vaisseaux, événements, fin de partie

**Files:**
- Modify: `docs/PFD.md` (§3.5, §3.7, §3.8)

**Interfaces:**
- Consumes: spec §6.2 ; vaisseaux §5.2 (Task 2), §7-8 (Task 3).

- [ ] **Step 1: §3.5 — vaisseaux**

Remplacer dans la table §3.5 la ligne `| Vaisseau cargo | transport interplanétaire |` par **trois lignes** :
```markdown
| Navette | transport intra-système (pods/slots) |
| IOS | transport interplanétaire (pods/slots) |
| SCG | transport interstellaire FTL |
```
(Conserver Sonde, Briseur d'astéroïde, Vaisseau d'assaut).

- [ ] **Step 2: §3.7 — quête segments**

Après la puce « Système de danger global Méthanoïde », ajouter :
```markdown
- Quête de fin de partie : **8 segments Hydroïdes** révélés par leur message crypto (jour 300) et éparpillés un par système extrasolaire → victoire lorsqu'ils sont tous récupérés.
```

- [ ] **Step 3: §3.8 — fin de partie**

Remplacer le §3.8 actuel par :
```markdown
### 3.8 Fin de partie

**Victoire** : 8 systèmes conquis + **8 segments Hydroïdes récupérés** (Atlantic, Chloé, Babylone, Hadrien, Romulus, Césius, Pliocène, Alpha) → clip de victoire.

**Défaite** (l'une des deux conditions) :
1. Perte de la Terre-Ville (invasion réussie) ;
2. Effondrement économique et militaire : plus aucune installation de production ni unité de combat opérationnelle.

Écran récap (partie, durée, statistiques).
```

- [ ] **Step 4: Vérifier la cohérence**

Lancer : `rg -n "extermination|pacification|recolonisation" docs/PFD.md` — aucune occurrence restante de l'ancienne victoire.

- [ ] **Step 5: Commit**

```bash
git add docs/PFD.md
git commit -m "docs(pfd): v0.2 vaisseaux transport, quête segments, fin de partie"
```

---

### Task 5: DATA.md — modèles vessels / VesselRuntime / défaite

**Files:**
- Modify: `docs/DATA.md` (§1.5, §2)

**Interfaces:**
- Consumes: spec §3, §6.3 ; §2.3/§5.2 GAMEPLAY (Task 1/2).

- [ ] **Step 1: Réécrire §1.5 `vessels.json`**

Remplacer la phrase de §1.5 `### 1.5 vessels.json — similaire (voir GAMEPLAY §5.2).` par :
```markdown
### 1.5 `vessels.json`

```jsonc
{
  "id": "ios",
  "name": "IOS (Interplanetary Ops)",
  "category": "transport",
  "capacity": 2500,               // t — masse maximale transportable
  "slots": { "tool": 1, "supply": 2, "cryo": 0 },
  "fuelType": "meh_fuel",         // meh_fuel (sol/intra) | hed_fuel (orbite/FTL)
  "fuelCostPerMassDay": 0.01,     // → coût = fond × (distance × masse)
  "range": "interplanetary",      // shuttle | intra | interplanetary | interstellar
  "travel": { "landDays": 2, "takeoffDays": 5 }   // navette seulement
}
```
```
> Note : `data/vessels.json` n'existe pas encore ; ce bloc documente le format **cible** (v2 des data, implémentation sim future).

- [ ] **Step 2: Réécrire `VesselRuntime` dans §2 (GameState)**

Remplacer le bloc `type VesselRuntime` actuel par :
```ts
type VesselRuntime = {
  id: string;
  templateId: string;         // référence data/vessels.json (cible)
  position: Position;         // { kind: "orbiting", planetId } | { kind: "transit", from, to, progress }
  modules: CargoSlot[];       // slots : 1 cargaison homogène par slot
  fuel: number;               // niveau de carburant restant (MeH/HeD)
  fuelType: "meh_fuel" | "hed_fuel";
  orders: Order[];            // envoi manuel ou routes de ravitaillement
  health: number;
};

type CargoSlot = {
  kind: "supply" | "tool" | "cryo";
  itemId?: string;            // ressource (supply) ou item (tool)
  quantity: number;           // max 250/chargement pour supply ; 1 équipe pour cryo
  crewTeamId?: string;        // si cryo
};
```

- [ ] **Step 3: Ajouter la note de détection défaite**

À la fin de §2 (après `VesselRuntime`), ajouter :
```markdown
> **Détection de défaite** (GameState) : les conditions GAMEPLAY §10 se lisent dans l'état — `flags` (Terre-Ville perdue, segments endommagés), état des installations de production (`caution` des structures), unités de combat opérationnelles (flotte). L'implémentation exacte relève de la sim (EPIC future).
```

- [ ] **Step 4: Vérifier la cohérence**

Lancer : `rg -n "cargo: Record|fuel: number" docs/DATA.md` — le VesselRuntime actuel (tonnage abstrait) remplacé ; le §2 ne doit contenir que le nouveau.

- [ ] **Step 5: Commit**

```bash
git add docs/DATA.md
git commit -m "docs(data): v0.2 vessels slots, VesselRuntime, détection défaite"
```

---

### Task 6: Consignation (DECISIONS, TRACE, DASHBOARD, RESEARCH)

**Files:**
- Modify: `docs/DECISIONS.md`, `docs/TRACE.md`, `docs/DASHBOARD.md`, `docs/RESEARCH.md`

**Interfaces:**
- Consumes: toutes les tâches précédentes (le jalon v0.2 est fait) ; spec §8 (K15).

- [ ] **Step 1: DECISIONS.md — décision K15**

Insérer une section `## Décision K15 — GAMEPLAY v0.2 : transport pods/slots, Hydroïdes narratifs, défaite spirale (approuvé 2026-09-18)` **avant** `## Journal des révisions` :
```markdown
## Décision K15 — GAMEPLAY v0.2 : transport pods/slots, Hydroïdes narratifs, défaite spirale (approuvé 2026-09-18)

**Contexte** : TODO court terme n°3 — arbitrer les écarts RESEARCH §15 (transport, Hydroïdes, défaite) et répercuter les tables.

**Choix** :
- **Transport hybride UX** : vaisseau = capacité (t) + slots ; slot homogène `{ ressource|item, quantité }` ; types supply (250/chargement) / tool (item `toolPod`) / cryo (équipe) ; masse = contrainte de charge ; carburant MeH/HeD = f(distance, masse) ; routage manuel + routes (pré-ACC), ACC/AOC automatisent.
- **Vaisseaux transporteurs** : Navette (1 slot, 100 t, MeH), IOS (3 slots, 2 500 t, MeH), SCG (3 slots, 5 000 t, HeD, FTL). Combat v0.1 conservé (remodelage v0.3).
- **Ressources : 16** d'origine (au lieu des 10 exotiques v0.1) — les data (`resources.json`, `items.json`) étaient déjà conformes.
- **Hydroïdes narratifs renforcés** : message crypto jour 300 + bulletins épisodiques ; 8 segments = artefacts à préserver (victoire).
- **Défaite spirale** : perte Terre-Ville OU effondrement (plus d'infra productive ni d'unité de combat) ; expansion Méthanoïde par « Prises de position » à partir d'un seuil M + guerre déclarée.

**Conséquences** : `GAMEPLAY.md` v0.2, `PFD.md` (§3.5/§3.7/§3.8), `DATA.md` (§1.5/§2) mis à jour ; les `data/*.json` et la sim restent inchangés (implémentation = EPIC transport/Lune et menace).
```

- [ ] **Step 2: DECISIONS.md — journal**

Ajouter en tête du tableau `## Journal des révisions` :
```markdown
| 2026-09-18 | K15 approuvé — GAMEPLAY v0.2 (transport pods/slots, Hydroïdes narratifs, défaite spirale) ; spec `2026-09-18-gameplay-v0.2-design.md` |
```

- [ ] **Step 3: TRACE.md — Session en cours**

Ajouter en tête de `docs/TRACE.md` (au-dessus de la dernière session) une entrée **Session 23** :
```markdown
## Session 23 — 2026-09-18 (GAMEPLAY v0.2 : arbitrages transport / Hydroïdes / défaite)

- Brainstorming 4 sections validées → spec `docs/superpowers/specs/2026-09-18-gameplay-v0.2-design.md` (commit `e173252`).
- Réécriture `GAMEPLAY.md` v0.2 : ressources 16, transport pods/slots (Navette/IOS/SCG), routage + ACC/AOC, IA Méthanoïde (Prises de position), timeline Hydroïdes + segments, nouveau § Défaite.
- `PFD.md` §3.5/§3.7/§3.8 ; `DATA.md` §1.5/§2 (vessels/VesselRuntime) ; note détection défaite.
- Décision K15 consignée. Sim et data json inchangés (implémentation = EPIC transport/Lune).
```

- [ ] **Step 4: DASHBOARD.md — TODO et compteur**

- Remplacer/barre le TODO court terme n°3 `GAMEPLAY v0.2 : arbitrer transport/Hydroïdes/défaite + répercuter les corrections tables v1.` par `~~GAMEPLAY v0.2 — arbitrages posés (spec + docs v0.2)~~ ✅`.
- Incrémenter `Sessions` si une ligne existe et mettre à jour `Commits` avec `git rev-list --count HEAD` après le commit.

- [ ] **Step 5: RESEARCH.md — arbitrage §15 « décidé »**

Dans §15, ajouter une ligne sous « **Arbitrages déjà tranchés** » :
```markdown
- ✅ **Ressources (16)** : l'original en a 16 (fer, aluminium, cuivre… + carburants composés MeH/HeD) — les « 10 exotiques » v0.1 sont abandonnées ; `GAMEPLAY.md` v0.2 et `data/resources.json` alignés.
- ✅ **Transport pods/slots hybride** (pas le tonnage abstrait) : voir GAMEPLAY §2.3.
- ✅ **Hydroïdes narratifs** (message + bulletins + segments) : voir GAMEPLAY §8.
- ✅ **Défaite = spirale** (perte d'infra, pas le seul « Terre détruite ») : voir GAMEPLAY §10, PFD §3.8.
```

- [ ] **Step 6: Vérifications**

Lancer :
- `git log --oneline -6` — 6 commits attendus (T1…T6) sur 4 fichiers (GAMEPLAY, PFD, DATA, + consignation).
- `rg -n "Version 0.2" docs/GAMEPLAY.md` — l'en-tête en v0.2.
- `rg -n "K15" docs/DECISIONS.md docs/TRACE.md docs/DASHBOARD.md docs/RESEARCH.md` — K15 présent partout où prévu.

- [ ] **Step 7: Commit**

```bash
git add docs/DECISIONS.md docs/TRACE.md docs/DASHBOARD.md docs/RESEARCH.md
git commit -m "docs: consignation GAMEPLAY v0.2 (K15, TRACE Session 23, DASHBOARD, RESEARCH)"
```

---

## Self-Review (rétro-inspection)

- **Spec coverage** : spec §1 (contexte) → en-tête T1 ; §2 (16 ressources) → T1 Step 2 ; §3 (transport) → T1 Step 4-5, T2 Step 4 ; §4 (Hydroïdes) → T3 Step 2 ; §5 (défaite) → T3 Step 1/4 ; §6.1-6.3 (répercussion tables) → T1/T2/T3 (GAMEPLAY), T4 (PFD), T5 (DATA) ; §8 (validation/consignation) → T6.
- **Placeholders** : aucune ligne « TBD/TODO » restante ; chaque step contient le texte exact à écrire.
- **Type consistency** : les noms d'items (`supply`/`tool`/`cryo`, `meh_fuel`/`hed_fuel`) sont cohérents avec `data/items.json`/`data/resources.json` ; les interfaces `CargoSlot`/`VesselRuntime` (T5) reproduisent le vocabulaire GAMEPLAY §2.3 (T1).
- **Écart assumé** : GAMEPLAY §5.2 « combat » conserve des coûts v0.1 provisoires (exotiques) marqués d'une note — recalcés à la v0.3, hors périmètre v0.2 (cohérent avec spec §7).